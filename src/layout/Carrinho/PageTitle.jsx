import React from "react";  
import classes from '../../Styles/carrinho.module.scss';
const PageTitle = () => {
  return (
    <div className={classes.pageTitle}>Seu Carrinho</div>
  );
};

export default PageTitle;