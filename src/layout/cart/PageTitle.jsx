import React from "react";  
import classes from '../../Styles/cart.module.scss';
const PageTitle = () => {
  return (
    <div className={classes.pageTitle}>Seu cart</div>
  );
};

export default PageTitle;